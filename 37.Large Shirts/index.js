function make_shirt(size = "Large", message = "Code is Life") {
    console.log(`The size of shirt is ${size} and it will have the message: "${message}" printed on it.`);
}
make_shirt();
function make_Shirt(size = "Medium", message = "Code is Life") {
    console.log(`The size of shirt is ${size} and it will have the message: "${message}" printed on it.`);
}
make_Shirt();
function Make_Shirt(size = "Small", message = "Keep Calm and Code On") {
    console.log(`The size of shirt is ${size} and it will have the message: "${message}" printed on it.`);
}
Make_Shirt();
export {};
